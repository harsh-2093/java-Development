public class Shapes {
    public double computeSquareArea(double length)
    {
        return length*length;
    }
    public double computeCircleArea(double length)
    {
        return 3.14*length*length;
    }
}
